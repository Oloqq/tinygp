def factorial(x):
    val = x
    for i in range(x):
        val *= i
    return val

def factorial(x):
    lim = cpy(x)

(R0 = x, R1)
LOAD R1 1
WHILE GT R0 1 DO
    LOAD R1 R1 * R0
    LOAD R0 SUB R0 1
END
RETURN R1